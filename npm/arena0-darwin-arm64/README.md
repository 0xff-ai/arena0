# @0xff-ai/arena0-darwin-arm64

The `arena0`, `arena0d`, and `cargo-arena0` prebuilt binaries for macOS 14 or
newer on Apple Silicon.
Installed automatically as an optional dependency of
[`@0xff-ai/arena0`](https://www.npmjs.com/package/@0xff-ai/arena0); do not
depend on it directly.
