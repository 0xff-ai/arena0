[package]
name = "arena0-sdk-macros"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Proc macros for arena0 program authoring: ABI glue, metadata, state management codegen"
publish = true
keywords = ["arena0", "wasm", "proc-macro", "sdk"]
categories = ["wasm", "development-tools::procedural-macro-helpers"]

[lib]
proc-macro = true

[dependencies]
proc-macro2 = { workspace = true }
quote = { workspace = true }
syn = { workspace = true }

[lints]
workspace = true
