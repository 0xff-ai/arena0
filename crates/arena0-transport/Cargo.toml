[package]
name = "arena0-transport"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Local transport and negotiation effects for arena0"
publish.workspace = true

[dependencies]
arena0-crypto = { path = "../arena0-crypto" }
arena0-program = { path = "../arena0-program" }
arena0-protocol = { path = "../arena0-protocol" }
arena0-wire = { path = "../arena0-wire" }

# Serialization / framing
borsh.workspace = true

# Async
tokio = { workspace = true }
async-trait.workspace = true
bytes.workspace = true

# Hashing / identity helpers
blake3.workspace = true
thiserror = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }

[lints]
workspace = true
